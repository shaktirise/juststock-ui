// AdSlider removed — stub left for compatibility.
// The ad slider implementation was intentionally removed per request.
import 'package:flutter/material.dart';

class AdSlider extends StatelessWidget {
  const AdSlider({super.key});

  @override
  Widget build(BuildContext context) {
    // Return nothing so the UI remains unchanged where AdSlider was used.
    return const SizedBox.shrink();
  }
}

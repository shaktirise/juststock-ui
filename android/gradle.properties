org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -Xlint:-options
android.useAndroidX=true
android.enableJetifier=true

# Enable Gradle performance features for faster builds
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
# Let Gradle pick optimal workers; remove hard cap of 1
# org.gradle.workers.max=1
org.gradle.vfs.watch=true

# Keep Kotlin compiler defaults; allow incremental/daemon for performance
# kotlin.compiler.execution.strategy=in-process
# kotlin.incremental=false
# kotlin.incremental.android=false
# kotlin.incremental.useClasspathSnapshot=false
